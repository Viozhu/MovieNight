query similarMovies($id: Int!) {
  similarMovies(id: $id) {
    id
    imdb_id
    poster_path
    title
    overview
    release_date
    runtime
    vote_average
    production_companies {
      name
    }
    genres {
      name
    }
  }
}
